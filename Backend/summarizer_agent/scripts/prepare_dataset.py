raise SystemExit(
    "prepare_dataset.py is a legacy local-prep script. The Azure-only workflow should use an existing Azure AI Search index as the KB source, not generate clinical_dataset.json locally."
)